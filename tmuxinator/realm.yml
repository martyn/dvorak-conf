project_name: realm
project_root: ~/dev/realm
socket_name: realm # Not needed.  Remove to use default socket
tabs:
  - gitrun:
      layout: main-vertical
      panes:
        - git pull
        - giles -s public/ #empty, will just run plain bash
  - edit: vim
